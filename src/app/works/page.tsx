"use client";

import styles from './works.module.css';

export default function Works() {
    return (
      <div>
        <h1 className={styles.title}>Works</h1>
        <p className={styles.description}>This is the works page.</p>
      </div>
    );
  }